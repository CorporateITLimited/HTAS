@model HoldingTaxWebApp.ViewModels.QueryCommon

@{
    ViewBag.Title = "Create";
}
@section Sheets{
    <link href="~/Content/formdesign.css" rel="stylesheet" />
}

<div class="subheader py-2 py-lg-6 subheader-transparent" id="kt_subheader">
    <div class="container d-flex align-items-center justify-content-between flex-wrap flex-sm-nowrap">
        <div class="d-flex align-items-center flex-wrap mr-1">
            <div class="d-flex align-items-baseline flex-wrap mr-5">
                <h5 class="text-dark font-weight-bold my-1 mr-5">গৃহকর প্রস্তুতকরণ/পুনঃ প্রস্তুতকরণ/চূড়ান্তকরণ</h5>
                <ul class="breadcrumb breadcrumb-transparent breadcrumb-dot font-weight-bold p-0 my-2 font-size-sm">
                    <li class="breadcrumb-item text-muted">
                        <a href="#" class="text-muted">নির্দিষ্ট সংখ্যক গৃহকরদাতাদের জন্য গৃহকরের তথ্য প্রস্তুতকরণ/পুনঃ প্রস্তুতকরণ/চূড়ান্তকরণ</a>
                    </li>
                </ul>
            </div>
        </div>
        <div class="d-flex align-items-center">
            <a onclick="return goBack();" class="btn btn-light-primary font-weight-bolder btn-sm"><i class="fas fa-arrow-left"></i>আগের পেজে যান</a>
        </div>
    </div>
</div>

<div class="d-flex flex-column-fluid">
    <div class="container">
        <div class="row mt-0 mt-lg-3">
            <div class="col-lg-12">
                <div class="card card-custom" id="kt_card_1">
                    <div class="card-header">
                        <h3 class="card-title"> <b>প্রথম ধাপ : </b> &nbsp;&nbsp;&nbsp; নতুন বছরের গৃহকর তৈরী করুন  </h3>
                        <div class="card-toolbar">
                            <div class="example-tools justify-content-center">
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="form-group row">
                            @Html.LabelFor(model => model.FinancialYearId, "আর্থিক বছর", htmlAttributes: new { @class = "col-lg-3 col-form-label text-lg-right" })
                            <div class="col-lg-7">
                                @Html.DropDownListFor(model => model.FinancialYearId, null, "-- নির্বাচন করুন --", new
                           {
                               @class = "form-control",
                               @name = "FinancialYearId",
                               @id = "FinancialYearId"
                           })
                                @Html.ValidationMessageFor(model => model.FinancialYearId, "", new { @class = "text-danger" })
                            </div>

                        </div>
                    </div>
                    <div class="card-footer">
                        <div class="text-center">
                            <button type="submit" id="btnSubmit" class="btn btn-outline-success btn-sm"><i class="fas fa-check"></i>গৃহকর তৈরী করুন</button>
                            <button type="button" onclick="window.location.href = '/HoldingTax/Index'" class="btn btn-outline-dark btn-sm"><i class="fas fa-arrow-left"></i>মূল তালিকায় ফিরে যান</button>
                        </div>
                        <div>
                            <span id="details_error"></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="row mt-0 mt-lg-3">
            <div class="col-lg-12">

                <div class="card card-custom" id="kt_card_1">
                    <div class="card-header">
                        <h3 class="card-title">
                            <b>দ্বিতীয় ধাপ :  </b> &nbsp;&nbsp;  গৃহকরদাতাদের জন্য গৃহকরের তথ্য পুনঃ প্রস্তুতকরণ &nbsp;&nbsp;
                        </h3>
                    </div>
                    <div class="card-body">
                        <div class="form-group row">
                            @Html.LabelFor(model => model.FinancialYearId_Three, "আর্থিক বছর", htmlAttributes: new { @class = "col-lg-1 col-form-label text-lg-right" })
                            <div class="col-lg-2">
                                @Html.DropDownListFor(model => model.FinancialYearId_Three, null, "-- নির্বাচন করুন --", new
                           {
                               @class = "form-control",
                               @name = "FinancialYearId_Three",
                               @id = "FinancialYearId_Three"
                           })
                            </div>

                            @Html.LabelFor(model => model.AreaId, "এলাকার নাম", htmlAttributes: new { @class = "col-lg-1 col-form-label text-lg-right" })
                            <div class="col-lg-2">
                                @Html.DropDownListFor(model => model.AreaId, null, "-- নির্বাচন করুন --", new
                           {
                               @class = "form-control",
                               @name = "AreaId",
                               @id = "AreaId"
                           })
                            </div>

                            @Html.LabelFor(model => model.PlotId, "প্লট নম্বর", htmlAttributes: new { @class = "col-lg-1 col-form-label text-lg-right" })
                            <div class="col-lg-2">
                                @Html.DropDownListFor(model => model.PlotId, null, "-- নির্বাচন করুন --", new
                           {
                               @class = "form-control",
                               @name = "PlotId",
                               @id = "PlotId"
                           })
                            </div>

                            <div class="col-lg-3">
                                <div class="text-center">
                                    <button type="button" id="btnSearch" class="btn btn-outline-primary btn-sm"><i class="fas fa-search"></i>অনুসন্ধান</button>
                                    <button class="btn btn-outline-dark btn-sm" id="reset_button"><i class="fas fa-trash-restore"></i>রিসেট</button>
                                </div>
                            </div>

                        </div>
                        <br />
                        <h5 class="card-title"><span style="color: forestgreen; text-decoration:underline;"> যেসব গৃহকরদাতাদের গৃহকরের তথ্য পুনঃ প্রস্তুত করবেন তাদের পাশে টিক দিন </span> </h5>
                        <table class="table table-bordered table-hover table-sm" width="100" id="holder_details">
                            <thead class="thead-light text-center">
                                <tr>
                                    <th>নং</th>
                                    <th>নাম</th>
                                    <th>আইডি নম্বর</th>
                                    <th>নেট করের পরিমাণ</th>
                                    <th>
                                        <input type="checkbox" id="chkHolder" />
                                        সব টিক দিন
                                    </th>
                                </tr>
                            </thead>
                            <tbody></tbody>
                        </table>
                    </div>
                    <div class="card-footer">
                        <div class="text-center">
                            <button type="submit" id="btnSubmitRegenerate" class="btn btn-outline-success btn-sm"><i class="fas fa-edit"></i>গৃহকর হালনাগাদ করুন</button>
                            <button type="button" onclick="window.location.href = '/HoldingTax/Index'" class="btn btn-outline-dark btn-sm"><i class="fas fa-arrow-left"></i>মূল তালিকায় ফিরে যান</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-0 mt-lg-3">
            <div class="col-lg-12">
                <div class="card card-custom" id="kt_card_1">
                    <div class="card-header">
                        <h3 class="card-title"> <b>শেষ ধাপ :  </b> &nbsp;&nbsp; প্রস্তুতকৃত গৃহকর চূড়ান্তকরণ</h3>
                        <div class="card-toolbar">
                            <div class="example-tools justify-content-center">
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="form-group row">
                            @Html.LabelFor(model => model.FinancialYearId_Two, "আর্থিক বছর", htmlAttributes: new { @class = "col-lg-3 col-form-label text-lg-right" })
                            <div class="col-lg-7">
                                @Html.DropDownListFor(model => model.FinancialYearId_Two, null, "-- নির্বাচন করুন --", new
                           {
                               @class = "form-control",
                               @name = "FinancialYearId_Two",
                               @id = "FinancialYearId_Two"
                           })
                                @Html.ValidationMessageFor(model => model.FinancialYearId_Two, "", new { @class = "text-danger" })
                            </div>

                        </div>
                    </div>
                    <div class="card-footer">
                        <div class="text-center">
                            <button type="button" id="btnFinalize" class="btn btn-outline-success btn-sm"><i class="fas fa-check"></i>গৃহকর চূড়ান্ত করুন </button>
                            <button type="button" onclick="window.location.href = '/HoldingTax/Index'" class="btn btn-outline-dark btn-sm"><i class="fas fa-arrow-left"></i>মূল তালিকায় ফিরে যান</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {

    <script>
        $('#FinancialYearId').select2({
            placeholder: "--নির্বাচন করুন--",
            allowClear: true
        });
    </script>
    <script>
        $("#btnSubmit").click(function (e) {

            $("#btnSubmit").addClass('spinner spinner-rgb(0, 0, 0) spinner-right')
            debugger;
            var fy = $('#FinancialYearId').val();
            if (fy === "0" || fy === '') {
                Swal.fire('ত্রুটি!', 'আর্থিক বছর নির্বাচন করুন', 'info');
            }

            $.ajax({
                type: 'GET',
                url: '/HoldingTax/GenerateTax',
                data: "FinancialYearId=" + fy,
                contentType: 'application/json',
                success: function (status) {

                    if (status === 202) {
                        Swal.fire({
                            text: "সফলভাবে সংযুক্ত করা হয়েছে",
                            icon: "success",
                            buttonsStyling: false,
                            confirmButtonText: "সফলভাবে সম্পন্ন",
                            customClass: {
                                confirmButton: "btn font-weight-bold btn-success",
                            }
                        });
                        setTimeout(function () { window.location.href = "/HoldingTax/Index"; }, 2000);
                    }
                    else if (status === 300) {
                        Swal.fire({
                            text: " নতুন করে হোল্ডিং ট্যাক্স তৈরি করার জন্য কোনো হোল্ডার পাওয়া যায়নি  ",
                            icon: "error",
                            buttonsStyling: false,
                            //confirmButtonText: " Canceled ",
                            customClass: {
                                confirmButton: "btn font-weight-bold btn-danger",
                            }
                        });
                        $('#details_error').text(status);
                        //KTUtil.scrollTop();
                    }
                    else {
                        Swal.fire({
                            text: "Unknown Error ",
                            icon: "error",
                            buttonsStyling: false,
                            //confirmButtonText: "  ERROR  ",
                            customClass: {
                                confirmButton: "btn font-weight-bold btn-danger",
                            }
                        });
                        $('#details_error').text(d.status);
                    }
                },
                error: function (error) {
                    console.log(error);
                }
                
            });
            //$("#btnSubmit").removeClass('spinner spinner-rgb(0, 0, 0) spinner-right')
        })

    </script>

    @*added by faisal*@
    <script>
        jQuery(document).ready(function () {
            $('#FinancialYearId_Two').select2({
                placeholder: "--নির্বাচন করুন--",
                allowClear: true
            });

            $("#btnFinalize").click(function (e) {
                e.preventDefault();
                var fy = $('#FinancialYearId_Two').val();
                var isAllValid = true;
                if (fy === "0" || fy === '') { isAllValid = false; }

                if (!isAllValid) {
                    Swal.fire('ত্রুটি!', 'আর্থিক বছর নির্বাচন করুন', 'info');
                }
                else {
                    Swal.fire({
                        title: 'আপনি কি নির্বাচিত আর্থিক বছরের জন্য গৃহকর চূড়ান্তকরণ করতে চান? ',
                        text: 'নির্বাচিত আর্থিক বছরের জন্য গৃহকর একবার চূড়ান্ত হয়ে গেলে পরবর্তীতে হালনাগাদ সম্ভব নয়',
                        icon: 'warning',
                        showCancelButton: true,
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        confirmButtonText: 'সাবমিট করুন',
                        cancelButtonText: 'বাতিল করুন'
                    }).then((result) => {
                        if (result.value) {
                            $.ajax({
                                type: 'GET',
                                url: '/HoldingTax/FinalizeHoldingTax',
                                data: "FinancialYearId=" + fy,
                                contentType: 'application/json',
                                success: function (status) {

                                    if (status === 202) {
                                        Swal.fire({
                                            text: "সফলভাবে গৃহকর মূল্যায়ন চূড়ান্তকরণ করা হয়েছে",
                                            icon: "success",
                                            buttonsStyling: false,
                                            confirmButtonText: "সফলভাবে সম্পন্ন",
                                            customClass: {
                                                confirmButton: "btn font-weight-bold btn-success",
                                            }
                                        });
                                        setTimeout(function () { window.location.href = "/HoldingTax/Index"; }, 2000);
                                    }
                                    else if (status === 409) {
                                        Swal.fire({
                                            text: " নির্বাচিত আর্থিক বছরের জন্য এখনো কোনো গৃহকর প্রস্তুত কর হয়নি",
                                            icon: "error",
                                            buttonsStyling: false,
                                            confirmButtonText: "বাতিল করা হয়েছে",
                                            customClass: {
                                                confirmButton: "btn font-weight-bold btn-danger",
                                            }
                                        });
                                        KTUtil.scrollTop();
                                    }
                                    else {
                                        Swal.fire({
                                            text: status,
                                            icon: "error",
                                            buttonsStyling: false,
                                            confirmButtonText: " বাতিল করা হয়েছে ",
                                            customClass: {
                                                confirmButton: "btn font-weight-bold btn-danger",
                                            }
                                        });
                                    }
                                },
                                error: function (error) {
                                    console.log(error);
                                }
                            });
                        }
                    });
                }
            });
        });

    </script>

    <script>
        jQuery(document).ready(function () {
            $('#FinancialYearId_Three').select2({
                placeholder: "--নির্বাচন করুন--",
                allowClear: true
            });
            $('#AreaId').select2({
                placeholder: "--নির্বাচন করুন--",
                allowClear: true
            });
            $('#PlotId').select2({
                placeholder: "--নির্বাচন করুন--",
                allowClear: true
            });

            $("#AreaId").change(function () {
                var areaId = $("#AreaId").val();
                var json = {
                    AreaId: areaId
                };
                $.ajax({
                    url: '@Url.Action("GetAllPlotByAreaId", "HoldingTax")',
                    type: "POST",
                    contentType: "application/json; charset=utf-8",
                    data: JSON.stringify(json),
                    success: function (data) {
                        if (data.length === 0) {
                            Swal.fire(
                                'পাওয়া যায়নি!', 'নির্বাচিত এলাকার জন্য কোনো প্লট পাওয়া যায়নি', 'info'
                            )
                        }
                        else {
                            $("#PlotId").empty();
                            $("#PlotId").append("<option value=''>--নির্বাচন করুন--</option>");
                            $.each(data, function (index, row) {
                                $("#PlotId").append("<option value='" + row.PlotId + "'>" + row.PlotNo + "</option>");
                            });
                        }
                    },
                    error: function () {
                        Swal.fire(
                            'পাওয়া যায়নি!', 'নির্বাচিত এলাকার জন্য কোনো প্লট পাওয়া যায়নি', 'error'
                        )
                    }
                });
            });

            function incrementByOne(index) {
                var i = index + 1;
                return i
            }

            $("#btnSearch").click(function (e) {
                e.preventDefault();
                var holder_details_table = $('#holder_details tbody');

                var financialYearId = parseInt($("#FinancialYearId_Three").val()) || 0;
                var areaId = parseInt($("#AreaId").val()) || 0;
                var plotId = parseInt($("#PlotId").val()) || 0;

                if (financialYearId === 0)
                {
                    Swal.fire({
                        title: "আর্থিক বছর নির্বাচন করুন!",
                        icon: "error",
                        buttonsStyling: false,
                        confirmButtonText: "আর্থিক বছর নির্বাচন করুন",
                        customClass: {
                            confirmButton: "btn font-weight-bold btn-danger",
                        }
                    });
                    return;
                }

                var json = {
                    FinancialYearId: financialYearId,
                    AreaId: areaId,
                    PlotId: plotId
                };

                holder_details_table.empty();

                $.ajax({
                    url: '@Url.Action("GetHolderForRegenerateTAX", "HoldingTax")',
                    type: "POST",
                    contentType: "application/json; charset=utf-8",
                    data: JSON.stringify(json),
                    success: function (data) {
                        if (data.length === 0) {
                            Swal.fire({
                                title: "তথ্য পাওয়া যায়নি!",
                                text: "কোনো গৃহকরদাতার তথ্য পাওয়া যায়নি",
                                icon: "info",
                                buttonsStyling: false,
                                confirmButtonText: "সঠিক তথ্য প্রদান করুন",
                                customClass: {
                                    confirmButton: "btn font-weight-bold btn-danger",
                                }
                            });
                        }
                        else {
                            $(data).each(function (index, upd) {
                                holder_details_table.append('<tr>' +
                                    '<input type="hidden" value=' + upd.HoldingTaxId + ' class="HoldingTaxId" id="HoldingTaxId" />' +
                                    '<input type="hidden" value=' + upd.HolderId + ' class="HolderId" id="HolderId" />' +
                                    '<td>' + incrementByOne(index) +'</td>'+
                                    '<td class="">' + upd.HolderName + '</td>' +
                                    '<td class="text-center">' + upd.HolderNo + '</td>' +
                                    '<td class="text-center">' + upd.NetTaxPayableAmount + '</td>' +
                                    '<td class="text-center"> <input type="checkbox" class="holding_tax_id_status" value="false" /> </td>' +
                                    '</tr>');
                            });
                        }
                    },
                    error: function (error) {
                        Swal.fire(
                            'কোনো গৃহকরদাতা পাওয়া যায়নি', ' ' + error.Message, 'error'
                        )
                    }
                });
            });

            $("#holder_details tbody").on('change', '.holding_tax_id_status', function () {
                var currentRow = $(this).closest("tr");
                if (currentRow.find('.holding_tax_id_status').is(":checked")) {
                    currentRow.find('.holding_tax_id_status').val("true");
                    currentRow.find('.holding_tax_id_status').prop('checked', true);
                } else {
                    currentRow.find('.holding_tax_id_status').val("false");
                    currentRow.find('.holding_tax_id_status').prop('checked', false);
                }
            });

            $("#reset_button").click(function (e) {
                e.preventDefault();
                window.location.href = '/HoldingTax/create';
            });


            $("#btnSubmitRegenerate").click(function (e) {
                e.preventDefault();
                Swal.fire({
                    title: 'আপনি কি টিক দেয়া গৃহকরদাতাদের গৃহকরের তথ্য পুনঃ প্রস্তুত করতে চান?',
                    text: 'যদি পুনঃ প্রস্তুত করেন তাহলে পূর্ববর্তী তথ্য সব মুছে যাবে এবং নতুন তথ্য হালনাগাদ হবে ',
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#3085d6',
                    cancelButtonColor: '#d33',
                    confirmButtonText: 'সাবমিট করুন',
                    cancelButtonText: 'বাতিল করুন'
                }).then((result) => {
                    if (result.value) {

                        var list = [];
                        $('#holder_details tbody tr').each(function (index, ele) {
                            var financialYearId = parseInt($('#FinancialYearId_Three option:selected').val()) || 0;
                            var holderId = parseInt($('.HolderId', this).val()) || 0;
                            var holdingTaxId = parseInt($('.HoldingTaxId', this).val()) || 0;
                            var holdingTaxIdStatus = $('.holding_tax_id_status', this).val();
                            var status_list = {
                                HolderId: holderId,
                                FinancialYearId: financialYearId,
                                HoldingTaxId: holdingTaxId,
                                HoldingTaxIdStatus: holdingTaxIdStatus
                            }
                            list.push(status_list);
                        });

                        var data = {
                            Items: list
                        }

                        $.ajax({
                            type: 'POST',
                            url: '@Url.Action("RegenerateTaxUpdate", "HoldingTax")',
                            data: JSON.stringify(data),
                            contentType: 'application/json',
                            success: function (d) {
                                if (d.status === "success") {
                                    list = [];
                                    $('#holder_details tbody').empty();
                                    Swal.fire({
                                        title: "সফলভাবে টিক দেয়া গৃহকরদাতাদের গৃহকরের তথ্য হালনাগাদ করা হয়েছে",
                                        icon: "success",
                                        buttonsStyling: false,
                                        confirmButtonText: "সফলভাবে হালনাগাদ করা হয়েছে",
                                        customClass: {
                                            confirmButton: "btn font-weight-bold btn-success",
                                        }
                                    });

                                    setTimeout(function () { window.location.href = "/HoldingTax/Index"; }, 2500);
                                }
                                else if (d.status !== "success") {
                                    Swal.fire({
                                        text: d.status,
                                        icon: "error",
                                        buttonsStyling: false,
                                        confirmButtonText: "বাতিল করা হয়েছে",
                                        customClass: {
                                            confirmButton: "btn font-weight-bold btn-danger",
                                        }
                                    });
                                }
                                else {
                                    Swal.fire({
                                        text: "অজানা ত্রুটি",
                                        icon: "error",
                                        buttonsStyling: false,
                                        confirmButtonText: "বাতিল করা হয়েছে",
                                        customClass: {
                                            confirmButton: "btn font-weight-bold btn-danger",
                                        }
                                    });
                                }
                            },
                            error: function (error) {
                                console.log(error);
                            }
                        });
                    }
                });
            });


            $("#chkHolder").change(function () {
                if ($(this).is(':checked')) {
                    $('.holding_tax_id_status').prop('checked', true);
                }
                else {
                    $('.holding_tax_id_status').prop('checked', false);
                }
            });

        });

    </script>
}